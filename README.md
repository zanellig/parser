# parser

To install dependencies:

```bash
npm install
```

To run:

```bash
node --experimental-transform-types index.ts --path "test_script.draw"
npm run index.ts --path [*.draw]
```

No AI agent was used to code this project. Only the Lexer was written with the help of GPT-5.5